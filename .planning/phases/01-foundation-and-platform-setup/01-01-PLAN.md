---
phase: 01-foundation-and-platform-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/main.tsx
  - src/index.css
  - src/features/foundation/pages/FoundationPage.tsx
  - src/features/foundation/data/phase-roadmap.ts
  - src/components/shell/AppShell.tsx
  - src/config/env.ts
  - src/lib/supabase.ts
autonomous: true
requirements:
  - FND-01
user_setup: []
must_haves:
  truths:
    - The application boots into a clean foundation experience aligned with the new product direction.
    - The source tree no longer depends on legacy placeholder modules to render the main app.
  artifacts:
    - src/App.tsx
    - src/features/foundation/pages/FoundationPage.tsx
    - src/components/shell/AppShell.tsx
    - src/config/env.ts
  key_links:
    - App mounts the new shell and foundation page.
    - Foundation UI reads stable project data from local feature modules.
---

<objective>
Replace the old mock-heavy app shell with a clean Phase 1 foundation experience.

Purpose: Reset the frontend baseline so future phases build on intentional structure instead of prototype leftovers.
Output: New app shell, foundation page, roadmap data module, and safe env/supabase contracts.
</objective>

<execution_context>
@C:/Users/juanc/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/juanc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-platform-setup/01-CONTEXT.md
@.planning/phases/01-foundation-and-platform-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the legacy application entry flow</name>
  <files>src/App.tsx, src/main.tsx, src/components/shell/AppShell.tsx, src/features/foundation/pages/FoundationPage.tsx, src/features/foundation/data/phase-roadmap.ts</files>
  <action>Remove the old login/dashboard placeholder app and create a new foundation-first interface that communicates the rebuilt project direction clearly.</action>
  <verify>npm run build</verify>
  <done>Main app renders through the new shell with no dependency on removed legacy pages.</done>
</task>

<task type="auto">
  <name>Task 2: Create safe configuration contracts</name>
  <files>src/config/env.ts, src/lib/supabase.ts</files>
  <action>Introduce typed env access and a non-crashing Supabase client contract so the app can run before secrets are fully configured.</action>
  <verify>npm run build</verify>
  <done>Frontend compiles and can surface missing env readiness without throwing at import time.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] App starts in dev mode on port 8081
- [ ] Legacy shell imports are gone from the main entry path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- New foundation page communicates current project status clearly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-platform-setup/01-01-SUMMARY.md`
</output>
